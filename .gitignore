# Full-resolution camera originals: kept locally, too large for git
images/gallery/_originals/
videos/_originals/
videos/_removed/
.DS_Store
.vscode/sftp.json
# Claude Code config. Kept local: .nojekyll disables Jekyll, so every
# tracked file is published, and this one carries absolute home-directory
# paths and the names of unrelated projects.
.claude/
